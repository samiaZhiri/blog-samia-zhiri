{% extends "layouts/default.twig" %}

{% block content %}    

{% for query in queries %}
 <div class="album py-5 bg-light">
        <div class="container">
          <div class="row justify-content-center">
           
            <div class="col-md-6">
              <div class="card mb-4 box-shadow">
                <img src="{{query.img}}" class="card-img-top" data-src="holder.js/100px225?theme=thumb&bg=55595c&fg=eceeef&text=Thumbnail" alt="Card image cap">
                <div class="card-body">
                <h5>{{query.title}}</h5>
                  <p>{{ query.created_at|date("d/m/Y à H:i") }}</p>
                  <p class="card-text">{{ query.content | striptags | slice(0, 100)  }}</p>
                  <div class="d-flex justify-content-between align-items-center">
                    <div class="btn-group">
                      <button type="button" class="btn btn-sm btn-outline-secondary"><a href="{{ route('home.show',{'id':query.id}) }}">Lire la suite</a></button>                     
                    
                  </div>
                </div>
              </div>
            </div>
             
           </div>
         </div>
  </div>
{% endfor %} 









    
  {# <div class="container ">
<div class=" row mb-2 "> 
    
    {% for query in queries %}
     <div class="col-md-6">

      <div class="card item-card card-block ">
      <h4 class="card-title text-right"><i class="material-icons"></i></h4>
    <img src="{{query.img}}" alt="#" >
        <h5 class="item-card-title mt-3 mb-3">{{query.title}}</h5>
        <p class="card-text">{{ query.created_at|date("d/m/Y à H:i") }}</p> 
        <p class="card-text">{{ query.content | striptags | slice(0, 100)  }}</p> 
        <a href="{{ route('home.show',{'id':query.id}) }}">Lire la suite</a> 
  </div>
    </div>  
   
    </div>
    {% endfor %} 
     
   </div> 
     </div>     #}
 {% endblock %}